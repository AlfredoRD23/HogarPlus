{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "mkdir -p \"${UPLOAD_DIR:-/data/uploads}\" && npx prisma db push --schema=apps/api/prisma/schema.prisma --skip-generate && node apps/api/dist/src/main.js"
  }
}
